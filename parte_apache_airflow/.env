# Environment variables for AlertaUTEC Airflow on ECS
# Configuración para AWS Academy

# AWS Configuration
AWS_DEFAULT_REGION=us-east-1
AWS_ACCOUNT_ID=858624593089

# Airflow Configuration
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=Admin2025!
AIRFLOW_ADMIN_EMAIL=admin@utec.edu.pe

# Database Configuration (will be fetched from SSM Parameter Store in ECS)
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow_admin:password@rds-endpoint:5432/airflow

# Webserver Secret Key (será generado automáticamente durante el deploy)
AIRFLOW__WEBSERVER__SECRET_KEY=to-be-generated-on-deploy

# DynamoDB Configuration
DYNAMODB_TABLE=Incidents

# S3 Configuration
S3_BUCKET=alerta-utec-airflow-858624593089

# ECS Configuration
ECS_CLUSTER_NAME=alerta-utec-airflow
ECR_REPOSITORY=alerta-utec-airflow

# Project Configuration
PROJECT_NAME=alerta-utec-airflow
